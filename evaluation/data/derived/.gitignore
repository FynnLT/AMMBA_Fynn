# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Node / Hardhat
node_modules/
artifacts/
cache/
typechain-types/
coverage/

# Environment / secrets
.env

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Office lock files (Excel writes ~$name.xlsx next to an open workbook)
~$*.xlsx
~$*.xlsm
~$*.docx

# Volta evidence output (results, not part of the artifact)
amm-smart-contract/volta_evidence*.csv

# Evaluation output (results, not part of the artifact)
evaluation/out/

# Derived evaluation data — regenerated from data/raw/ plus the extension
# seed recorded in the run manifest, so it is reproducible and not committed
evaluation/data/derived/
