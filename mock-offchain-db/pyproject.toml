[project]
name = "mock-offchain-db"
version = "0.1.0"
description = "In-memory mock of the GSY-DEX off-chain database (AMMBA PoC)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110,<1",
    "uvicorn[standard]>=0.29,<1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "httpx>=0.27,<1",
]
